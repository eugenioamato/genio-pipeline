[project]
name = "genio-pipeline"
version = "1.1.1"
description = "Pipelines implementation for Genio model"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["genio-core", "av", "tqdm", "pillow"]

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"
